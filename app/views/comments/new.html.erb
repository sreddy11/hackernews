<h1> Add comment </h1>

<%= form_for [@parent, @comment] do |f| %>
  <p>
    <%= f.label :body %>
    <%= f.text_area :body, :size => "30x10" %>
  </p>
  <p>
    <%= f.submit "Add Comment" %>
  </p>
<% end %>

