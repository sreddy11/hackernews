<h2><%= link_to @article.title, @article.url %><font size="1"> <%="( #{@article.domain} )" %> </h2>
<%= render("article", :article => @article) %>

<p> <%= button_to "Add comment", new_article_comment_path(@article), :method => :get %> </p>

<%= render("comments/comments", :comments => @article.comments) %>

<p>   
<%= link_to "Add comment", new_article_comment_path(@article), 
  { :class=>" btn btn-primary"}%> 
</p>

