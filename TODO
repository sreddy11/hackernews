put duplicate functions in application controller
Put shared views into partials
shorthen path syntax
change unless in views to if(combine with partial)
scope database queries
fix resource pluaralities
create method based off scope (vote_for_user(user) and voted_on_by_user?)
vote model => upvote?, downvote?
